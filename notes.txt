- SOIL plugin?
- lazyload font? zie #tech-team bwb
- zie pagina 25 voor toevoegen menus' (nodig voor footer, en meer?)

NADJI VRAGEN:
- huiststijl pagina 9:
  hoe h5 in light en regular uit te voeren? icm met agenda class oid op regular zetten dan?
- missende font awesome donate icon met euro?




development: browsersync:
- in openstatetheme2019_node_1 container voeg nginx-load-balancer IP toe voor openstate.eu aan hosts
   echo "192.168.144.3 openstate.eu" >> /etc/hosts
- op de host machine, in /etc/hosts voeg openstatetheme2019_node_1 IP toe voor openstate.eu; NEE toch? gewoon IP van wordpress

- in resources/assets/config.json chacnge '"proxyUrl": "https://localhost:3000"' to '"proxyUrl": "https://192.168.144.4:3000"' (IP of the openstatetheme2019_node_1 container), otherwise you get CORS/HMR errors in your console when browsersync tries to refresh

om HTTPS en wp-admin te checken bezoek dan gewoon openstate.eu.




# Verwijder alle acl permissies
sudo setfacl -Rbn open-state-theme-2019
# set deze acl permissies recursively
sudo setfacl -Rm group:coders:rwx open-state-theme-2019
# set deze acl permissies recursively als default voor nieuwe files en folders in alle huidige folders
sudo setfacl -Rdm group:coders:rwx open-state-theme-2019




font: https://fonts.google.com/specimen/IBM+Plex+Sans+Condensed?selection.family=IBM+Plex+Sans+Condensed:300,400,400i,700,700i


# Install font
docker exec openstatetheme2019_node_1 yarn add @ibm/plex

docker exec accountabilityhacktheme2019_node_1 yarn add @fortawesome/free-regular-svg-icons
en ook? docker exec accountabilityhacktheme2019_node_1 yarn add @fortawesome/fontawesome-svg-core

If add font awesome, dan per icon toevoegen in main.js!!!
// import then needed Font Awesome functionality
import { config, library, dom } from '@fortawesome/fontawesome-svg-core';
// import required icons
import { faCalendarPlus } from '@fortawesome/free-solid-svg-icons';

// allow usage in pseudo elements
config.searchPseudoElements=true;

// add the imported icons to the library
library.add(faCalendarPlus);

// tell FontAwesome to watch the DOM and add the SVGs when it detects icon markup
dom.watch();




bootstrap nav integreren in sage:
https://github.com/MWDelaney/sage-bootstrap4-navwalker

docker exec -it wordpress_app_1 bash
# Go to you theme folder, e.g.
cd wp-content/themes/open-state-theme-2019/
composer require mwdelaney/sage-bootstrap4-navwalker

in resources/views/partials/header.blade.php use:
    <button class="navbar-toggler float-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      menu
    </button>

    <nav class="nav-primary navbar navbar-light">
      @if (has_nav_menu('primary_navigation'))
        {!! wp_nav_menu(['theme_location' => 'primary_navigation', 'menu_class' => 'nav navbar-nav', 'container' => 'div', 'container_id' => 'navbarSupportedContent', 'container_class' => 'collapse navbar-collapse', 'walker' => new App\wp_bootstrap4_navwalker()]) !!}
      @endif
    </nav>




# OUDE PROBEERSELS, werkt niet goed icm browsersync
in resources/assets/config.json voeg devSsl certs toe

advanced: {                   
  browserSync: {              
    https: {                  
      key: config.devSsl.key, 
      cert: config.devSsl.crt 
    },                        
  }                           
}                             


"devSsl": {
  "key": "/certs/privkey.pem", 
  "crt": "/certs/fullchain.pem"
}


